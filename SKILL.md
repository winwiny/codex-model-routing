---
name: model-task-routing
license: MIT
description: 按任务难度选择 GPT-6 Astra、GPT-5.6 Sol、Terra、Luna 及思考档位，减少不必要的交接和返工。用户要求模型分工、子代理编码、省额度执行，或当前工作约定要求主代理分析验收并委派编码时使用。讨论、文档整理与只读核查可直接完成，不因使用此 Skill 而强制启动子代理。
---

# 按任务选择模型

目标是在达到验收标准的前提下，控制整个任务的额度消耗、耗时与返工。不要把低单价、少量输出或多层分工当成总成本更低的保证。

## 职责与默认选择

主代理负责理解目标、方案决策、范围控制和最终验收；所有具体编码、代码修复与测试代码编写交给当前任务内的子代理。讨论、文档编写、只读调查和运行已有检查可由主代理完成。

| 任务 | 模型 | 建议思考档位 | 执行方式 |
|---|---|---|---|
| 需求判断、方案取舍、最终验收 | GPT-6 Astra / `gpt-6-astra` | `medium`；困难问题 `high` | 主代理处理；不为了讨论而委派 |
| 明确的小改动、规则固定的批量修改 | GPT-5.6 Luna / `gpt-5.6-luna` | `low`；有局部逻辑用 `medium` | 直接交给一个执行代理 |
| 常规页面、交互、功能开发与测试 | GPT-5.6 Terra / `gpt-5.6-terra` | `medium`；较难局部问题 `high` | 作为日常编码主力 |
| 多模块关联、复杂故障、技术拆解与审查 | GPT-5.6 Sol / `gpt-5.6-sol` | `medium`；复杂推理 `high` | 可直接编码，也可组织有明确边界的执行任务 |

这些是默认建议，不是能力排名或固定耗费保证。以工具实际提供的模型 ID、档位和权限为准。不声称 Skill 可以自动切换当前主代理模型或修改界面设置；当前主代理不是 Astra 时如实说明，仅在有必要时建议切换。

不固定执行 Astra → Sol → Terra → Luna 四层交接。小任务省去经理层；复杂任务不强迫 Sol 把每一步继续下放。保留主代理的最终验收责任。

## 执行步骤

1. 明确结果和验收标准，按需要定位相关文件，保留现有修改。不要预先通读整个仓库。
2. 判断困难来自哪里：需求不清、资料缺失、环境故障或推理复杂。先补可获取的证据；提高思考档位不能补齐缺失信息。
3. 根据上表选择一个合适的执行模型。优先复用模型、任务上下文和状态都合适的空闲代理；不打断无关工作，不用错误模型冒充指定模型。
4. 发出简短任务单，推进主代理可独立完成的调查、验收准备或范围决策。遵守当前环境对启动子代理的限制；不要为了启动代理而制造无意义的并行工作。
5. 执行代理完成修改及对应验证。根据真实失败修复和复验；没有新证据时，不重复扩大测试范围。
6. 主代理检查关键差异和验证证据，必要时做独立检查，然后向用户交付。不能仅凭子代理一句“完成”就宣布通过。

## 最小任务单

只传必要上下文，不默认复制完整聊天历史或整份仓库。包含：

- 目标：要解决什么，预期行为是什么。
- 范围：允许修改的文件或模块、相关已有改动。
- 已知事实：复现方式、关键证据、已经尝试过什么。
- 约束：用户要求、权限边界、必须遵守的项目规则。
- 验收：必要检查及完成标准。
- 返回：修改摘要、验证结果、未验证项和阻塞；避免大段粘贴文件或日志。

明确要求执行代理读取适用的 AGENTS.md。使用可指定模型的委派工具时传入准确模型 ID 与受支持的思考档位，选择最少必要的上下文继承方式。只使用当前任务内的子代理，不擅自创建侧边栏新任务。

## 升级与并行

- 明显复杂的任务直接交给合适模型，不必先让 Luna 试错。
- 默认在同一问题连续两次实质修复仍未通过，或发现任务超出原定范围时，停止重复尝试并重新判断。缺信息就补信息；能力不足才考虑 Luna → Terra → Sol，必要时由主代理重新决策，也可以直接跳级。
- 升级时传递失败证据、已改文件和当前状态，避免下一位重复调查。主代理继续负责决策，不接手具体编码。
- `xhigh`、`max`、`ultra` 不作为默认档；仅在任务确有需要、工具支持且符合权限时使用。更高档位不保证更好结果，跨模型的同名档位也不能等同。
- 只并行处理可独立验收、写入范围不冲突的任务；共享文件指定一个修改负责人，有依赖就顺序执行。代理数量服从环境限制和实际收益，不为“公司层级”增加代理。
- 指定模型不可用时说明情况；有符合授权且可用的替代模型就明确告知并继续。无合规替代时先完成不受影响的工作，报告编码部分阻塞，不谎称调用成功。

## 成本与质量

区分 token 数量、套餐额度、credits 和 API 费用，不混用单位，不写死价格、智商分数或节省比例。需要价格时重新核对官方资料；不要每次执行都查询价格。

以完成并验收一个任务为比较单位，包含主代理、执行代理、交接、推理和返工。只有环境提供可信指标时才报告用量；没有数据就明确“未测量”，不要根据回复长度估算，也不额外编写计费系统。

减少无关上下文、重复读取、冗长汇报和重复测试；不能用省额度作为缩减需求、跳过必要验证或提前结束的理由。

本 Skill 不扩大对外发布、发送消息、敏感数据或其他操作的授权，也不修改用户长期偏好。需要修改规则时遵循用户明确要求。

## 路由例子

- “页脚加备案号并链接工信部”：Luna `low` 修改，核对号码、链接与页面；Astra 验收。
- “调整移动端咨询弹窗和复制失败提示”：Terra `medium` 实现，验证成功与失败行为；Astra 验收。
- “预约前后端偶发重复提交，定位并修复”：Sol `high` 调查与处理，必要时分配独立测试任务；Astra 验收。
- “分析官网还缺什么”：主代理只读核查并分析，不启动编码代理。
- “把这套分工写成文档”：主代理直接整理文档；不为写文档启动经理和员工。

## 最终回复

默认中文，先说结果。简要说明完成了什么、如何验证、仍有何限制。只有模型降级、任务升级或实际成本数据影响判断时才展开说明；不要逐层播报代理内部交接。
