---
name: model-task-routing
license: MIT
description: 按任务的明确程度、上下文复杂度和验收风险选择 GPT-6 Astra、GPT-5.6 Sol、Terra、Luna 及思考档位。用户要求模型分工、子代理编码、省额度执行或评估委派方式时使用。支持按需委派与全部编码委派；遵循用户和项目约定，不因使用 Skill 而强制启动代理。
---

# 按任务选择模型

目标是在达到验收标准的前提下，控制整个任务的额度消耗、耗时与返工。不要把低单价、少量输出或多层分工当成总成本更低的保证。

## 职责与默认选择

主代理负责理解目标、方案决策、范围控制和最终验收。先读取用户要求与适用的 AGENTS.md，确定执行方式：

- **按需委派（公共默认）**：比较交接成本与任务收益，简单修改可由当前主代理直接完成；有独立执行价值时再委派。不要为了小任务换模型、增加代理或制造并行。
- **全部编码委派**：用户或项目约定要求主代理不写代码时，所有编码、代码修复与测试代码编写交给子代理，包括小修改。主代理仍可讨论、编写文档、只读调查和运行已有检查。

上述模式是自然语言工作约定，不是新增的 Codex 配置项。明确的用户与项目约定优先于公共默认；缺少委派能力时，不得把“全部编码委派”静默降级为主代理编码。

| 任务 | 模型 | 建议思考档位 | 执行方式 |
|---|---|---|---|
| 需求判断、方案取舍、最终验收 | 主代理；用户偏好时使用 GPT-6 Astra / `gpt-6-astra` | 简单判断 `low`；一般 `medium`；困难 `high` | 按影响范围检查，不重复完整执行流程 |
| 要求明确、上下文有限、可独立验证的实现、修复与测试 | GPT-5.6 Luna / `gpt-5.6-luna` | 简单修改 `low`；涉及逻辑 `medium` | 不限于文案与机械修改 |
| 需理解现有项目、有局部不确定性或较多相关上下文的日常开发 | GPT-5.6 Terra / `gpt-5.6-terra` | `medium`；较难局部问题 `high` | 在上下文与判断需求增加时优先考虑 |
| 多模块关联、复杂故障、技术拆解与审查 | GPT-5.6 Sol / `gpt-5.6-sol` | `medium`；复杂推理 `high` | 可直接编码，也可组织有明确边界的执行任务 |
| 陌生系统、疑难根因、复杂迁移或失败代价高的任务 | GPT-6 Astra / `gpt-6-astra` | `medium` / `high` | 可直接选为执行子代理，无需先让其他模型试错 |

这些是待验证的默认起点，不是跑分已证明的最优档位、统一能力排名或耗费保证。按任务明确程度、上下文复杂度、结果可验证性和失败代价判断，不只按文件数量分配。以工具实际提供的模型 ID、档位和权限为准；不声称能自动切换当前主代理模型或修改界面设置。

不固定执行 Astra → Sol → Terra → Luna 四层交接。主代理已经消除的不确定性，不再让 Sol 重做分析。小任务省去经理层；复杂任务不强迫 Sol 继续下放。主代理不编码与 Astra 模型不能编码是两件事，Astra 子代理仍可执行。

## 执行步骤

1. 明确结果和验收标准，按需要定位相关文件，保留现有修改。不要预先通读整个仓库。
2. 判断困难来自哪里：需求不清、资料缺失、环境故障或推理复杂。先补可获取的证据；提高思考档位不能补齐缺失信息。
3. 确定直接执行或委派。直接执行时使用当前模型，不谎称已切换；委派时根据上表选模型，优先复用模型、上下文和状态都合适的空闲代理，不打断无关工作。
4. 委派时发出简短任务单，推进主代理可独立完成的调查、验收准备或范围决策。遵守当前环境的代理启动限制，不制造无意义的并行工作。
5. 执行者完成修改及对应验证。根据真实失败修复和复验；没有新证据时，不重复扩大测试范围。
6. 主代理检查关键差异和验证证据，必要时做独立检查，然后交付。小修改直接核对结果，复杂行为检查关键路径；不能仅凭子代理一句“完成”就宣布通过。

## 最小任务单

只传必要上下文，不默认复制完整聊天历史或整份仓库。包含：

- 目标：要解决什么，预期行为是什么。
- 范围：允许修改的文件或模块、相关已有改动。
- 已知事实：复现方式、关键证据、已经尝试过什么。
- 约束：用户要求、权限边界、必须遵守的项目规则。
- 验收：必要检查及完成标准。
- 返回：修改摘要、验证结果、未验证项和阻塞；避免大段粘贴文件或日志。

明确要求执行代理读取适用的 AGENTS.md。使用可指定模型的委派工具时传入准确模型 ID 与受支持的思考档位，选择最少必要的上下文继承方式。只使用当前任务内的子代理，不擅自创建侧边栏新任务。

## 升级与并行

- 明显复杂的任务直接交给合适模型，不必先让 Luna 试错。
- 发现理解错误、任务超出能力或范围变化时，立即重新判断，不必等待第二次失败。“连续两次实质修复仍未通过”只是触发复盘的经验提醒，不是必须重试两次、自动换模型或官方最佳阈值。
- 缺信息就补证据；环境、权限或授权问题不能靠换模型解决。能力不足时可选择 Terra、Sol 或 Astra，按问题直接跳级，不必逐级尝试。
- 升级时传递失败证据、已改文件和当前状态，避免重复调查。全部编码委派模式下，主代理继续决策，不接手具体编码。
- `xhigh`、`max`、`ultra` 不作为默认档；仅在任务确有需要、工具支持且符合权限时使用。更高档位不保证更好结果，跨模型的同名档位也不能等同。
- 只并行处理可独立验收、写入范围不冲突的任务；共享文件指定一个修改负责人，有依赖就顺序执行。代理数量服从环境限制和实际收益，不为“公司层级”增加代理。
- 指定模型不可用时说明情况；有符合授权且可用的替代模型就明确告知并继续。无合规替代时先完成不受影响的工作，报告编码部分阻塞，不谎称调用成功。

## 成本与质量

区分 token 数量、套餐额度、credits 和 API 费用，不混用单位，不写死价格、智商分数或节省比例。需要价格时重新核对官方资料；不要每次执行都查询价格。

以完成并验收一个任务为比较单位，包含主代理、执行代理、交接、推理和返工。只有环境提供可信指标时才报告用量；没有数据就明确“未测量”，不要根据回复长度估算，也不额外编写计费系统。

减少无关上下文、重复读取、冗长汇报和重复测试；不能用省额度作为缩减需求、跳过必要验证或提前结束的理由。

本 Skill 不扩大对外发布、发送消息、敏感数据或其他操作的授权，也不修改用户长期偏好。需要修改规则时遵循用户明确要求。

需要解释设计或复核跑分时，再读取 [设计依据与验证边界](docs/evidence.md)。日常执行不必加载该文件。基准条件、模型版本和思考档位可能不同，不能从最高分推断低档表现；尚未验证的节省效果如实说明。

## 路由例子

- “页脚加备案号并链接工信部”：按需模式可直接修改；全部委派模式交 Luna `low`。主代理核对号码、链接与页面。
- “按已明确的规则增加表单校验，并补充现有测试”：若上下文有限，Luna `medium` 可承担实现和测试；无需自动加一层 Sol。
- “梳理现有移动端咨询弹窗，再修复复制失败逻辑”：需要理解组件状态时，Terra `medium` 实现并验证成功与失败行为。
- “预约前后端偶发重复提交，定位并修复”：Sol `high` 调查与处理，必要时分配独立测试任务。
- “陌生系统的复杂迁移出现疑难数据一致性问题”：按具体风险与证据，可直接交 Astra `medium` / `high` 子代理；主代理按项目要求验收。
- “分析官网还缺什么”：主代理只读核查并分析，不启动编码代理。
- “把这套分工写成文档”：主代理直接整理文档；不为写文档启动经理和员工。

## 最终回复

默认中文，先说结果。简要说明完成了什么、如何验证、仍有何限制。只有模型降级、任务升级或实际成本数据影响判断时才展开说明；不要逐层播报代理内部交接。
